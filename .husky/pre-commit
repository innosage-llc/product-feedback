#!/usr/bin/env sh

branch="$(git symbolic-ref --short HEAD 2>/dev/null || true)"
if [ "$branch" = "main" ]; then
  echo "Direct commits on main are forbidden. Create a custom branch and open a PR."
  exit 1
fi
